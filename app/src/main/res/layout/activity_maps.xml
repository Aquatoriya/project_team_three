<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
        xmlns:app="http://schemas.android.com/apk/res-auto" xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        xmlns:android="http://schemas.android.com/apk/res/android">

    <fragment
            xmlns:tools="http://schemas.android.com/tools"
            xmlns:map="http://schemas.android.com/apk/res-auto"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:id="@+id/map"
            tools:context=".MapsActivity"
            android:name="com.google.android.gms.maps.SupportMapFragment"/>

    <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:layout_marginTop="10dp"
            android:layout_marginRight="10dp"
            android:layout_marginLeft="10dp"
            android:background="#fff"
            android:id="@+id/relLayouyt1"
    >
        <ImageView
                android:layout_width="15dp"
                android:layout_height="15dp"
                android:id="@+id/ic_magnify"
                android:layout_centerVertical="true"
                android:layout_marginLeft="10dp"
                android:src="@drawable/ic_magnify"
                android:layout_alignParentStart="true" android:layout_marginStart="10dp"/>


            <ImageView
                android:layout_width="15dp"
                android:layout_height="15dp"
                android:id="@+id/ic_clear"
                android:layout_centerVertical="true"
                android:src="@drawable/ic_clear"
                android:layout_alignParentEnd="true"
                android:layout_marginRight="10dp"
            />

        <AutoCompleteTextView
                android:layout_width="510dp"
                android:layout_height="match_parent"
                android:layout_toRightOf="@id/ic_magnify"
                android:layout_marginLeft="10dp"
                android:layout_centerVertical="true"
                android:textSize="15sp"
                android:textColor="#000"
                android:id="@+id/input_search"
                android:background="@null"
                android:hint="Enter a name"
                android:imeOptions="actionSearch"
                android:inputType="text"
                android:singleLine="true"
                android:completionThreshold="1"
                android:layout_toStartOf="@+id/ic_clear" android:layout_marginEnd="10dp"
                android:layout_toEndOf="@+id/ic_magnify" android:layout_marginStart="10dp"/>



    </RelativeLayout>

</RelativeLayout>
